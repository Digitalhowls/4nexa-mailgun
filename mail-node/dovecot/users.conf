# ─── users.conf — Base de usuarios Dovecot ───────────────────────────────
# Generado automáticamente por el Node Agent — NO EDITAR MANUALMENTE
# Formato: usuario@dominio:{ARGON2ID}<hash>:::::userdb_quota_rule=*:bytes=<N>
#
# Ejemplo:
# usuario@empresa.com:{ARGON2ID}$argon2id$v=19$m=65536,...:::::userdb_quota_rule=*:bytes=5368709120
