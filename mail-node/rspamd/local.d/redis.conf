# ─── redis.conf — Conexión de Rspamd a Redis ─────────────────────────────
# 4nexa Mailgun — Mail Node
# Redis se usa para Bayes, neural network, ratelimit, fuzzy y cuarentena

redis {
  servers = "redis:6379";
  # Sin contraseña en entorno local; en producción: password = "secret";
}
