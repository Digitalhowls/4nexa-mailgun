# ─── logging.inc — Configuración de logging Rspamd ───────────────────────
# 4nexa Mailgun — Mail Node

logging {
  type = "console";
  level = "notice";
  # En producción cambiar a "warning" para reducir ruido
}
